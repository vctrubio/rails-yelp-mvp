<%= simple_form_for @restaurant do |f| %>

  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.input :category, collection: Restaurant::CATEGORIES %>
  <%= f.submit "add a restaurant", class: "btn btn-primary" %>
<% end %>


<%# creating a new restaurant! so no need for review? %>